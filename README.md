# Logger
A simple Java logger. This logger has multiple logging levels and can be customized for use within your application.


To use: 

Simply call Logger.log. Pass in the level you would like that message logged at and the messsage that you would like logged. The class from which the log was initiated from, , the datetime, and the log level for that message will automatically be added to the output.
